interface:
  display_name: "Requirement Replacement Guard"
  short_description: "Prevent retired requirements from returning"
  default_prompt: "Use $requirement-replacement-guard to replace or remove behavior without leaving stale paths or assumptions."

policy:
  allow_implicit_invocation: true
